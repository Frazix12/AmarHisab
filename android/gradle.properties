# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# ═══════════════════════════════════════════════════════════════════════════════
# 🚀 SUPER FAST BUILD OPTIMIZATIONS
# ═══════════════════════════════════════════════════════════════════════════════

# Specifies the JVM arguments used for the daemon process.
# Aggressive memory allocation + JIT compilation optimizations
org.gradle.jvmargs=-Xmx16G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8 -XX:+UseParallelGC -XX:MaxGCPauseMillis=50

# Enable Gradle Daemon (keeps JVM warm between builds - huge speed boost)
org.gradle.daemon=true

# Enable parallel project execution (builds independent modules simultaneously)
org.gradle.parallel=true

# Enable configuration caching (reuses configuration phase results)
org.gradle.configuration-cache=true

# Enable build caching (reuses task outputs from previous builds)
org.gradle.caching=true

# Enable file system watching (detects file changes instantly)
org.gradle.vfs.watch=true

# Prioritize interactive builds (speeds up ./gradlew commands)
org.gradle.priority=high

# Run only configuration for requested tasks (skip unnecessary config)
org.gradle.configureondemand=true

# Kotlin daemon configuration for faster Kotlin compilation
kotlin.daemon.jvmargs=-Xmx4G -XX:+UseParallelGC

# Kotlin incremental compilation (only recompile changed code)
kotlin.incremental=true

# Use Kotlin compiler caching
kotlin.caching.enabled=true

# Disable Kotlin daemon fallback (faster failure detection)
kotlin.daemon.useFallbackStrategy=false

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# For FAST DEBUG builds, use only your device's architecture (arm64-v8a for modern phones)
# For RELEASE builds, use all: armeabi-v7a,arm64-v8a,x86,x86_64
# Override from CLI: ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=arm64-v8a

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Use this property to enable edge-to-edge display support.
# This allows your app to draw behind system bars for an immersive UI.
# Note: Only works with ReactActivity and should not be used with custom Activity.
edgeToEdgeEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false

# Specifies whether the app is configured to use edge-to-edge via the app config or plugin
# WARNING: This property has been deprecated and will be removed in Expo SDK 55. Use `edgeToEdgeEnabled` or `react.edgeToEdgeEnabled` to determine whether the project is using edge-to-edge.
expo.edgeToEdgeEnabled=true
